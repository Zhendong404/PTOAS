// RUN: ptoas --pto-arch a5 --enable-graph-sync-solver --graph-sync-solver-event-id-max=64 %S/tinsert_a5_pipe_selection.pto | FileCheck %s

// CHECK-LABEL: AICORE void tinsert_acc_mat_pipeline(
// CHECK: set_flag(PIPE_M, PIPE_FIX, EVENT_ID{{[0-9]+}});
// CHECK-NOT: set_flag(PIPE_M, PIPE_MTE3, EVENT_ID{{[0-9]+}});
// CHECK: wait_flag(PIPE_M, PIPE_FIX, EVENT_ID{{[0-9]+}});
// CHECK: TINSERT(
// CHECK: set_flag(PIPE_FIX, PIPE_MTE1, EVENT_ID{{[0-9]+}});
// CHECK-NOT: set_flag(PIPE_MTE3, PIPE_MTE1, EVENT_ID{{[0-9]+}});
// CHECK: wait_flag(PIPE_FIX, PIPE_MTE1, EVENT_ID{{[0-9]+}});
